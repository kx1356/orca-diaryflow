---
description: 每次加功能必须同步更新插件内嵌使用教程
alwaysApply: true
---

# 插件使用教程必须随功能更新

交付任何**面向用户的新功能 / 行为变更 / UI 入口**时，同一轮内维护插件内嵌「使用说明」：

1. 更新 `src/orca-blocks.js` 中的 `DF_TUTORIAL_TEXT`（条目化、写清入口与规则；勿写 `#标签` 字样；勿用 `-` 列表前缀）。
2. **递增** `DF_TUTORIAL_CONTENT_VER`（字符串即可），以便已安装用户下次打开插件时同步说明块正文。
3. 在 `docs/开发记录.md` 该版本条目中写一句「教程已更新」。

## 范围

- 适用：归档、回收站、分页、FAB、评论、编辑路径等用户能感知的变化。
- 不适用：纯内部重构、构建/CI、不影响用法的 bugfix（除非用法表述已过时）。

## 反例

- 只改代码与 README，不改 `DF_TUTORIAL_TEXT`
- 改了文案但忘记 bump `DF_TUTORIAL_CONTENT_VER`（老用户仍看到旧说明）
